pipeline {
    agent any
    stages {
        stage('mk dir') {
            steps {
                dir ('home\ubuntu')
                 'cd home\ubuntu'
                 'mkdir dockerSite'
                 'git clone https://github.com/NazarServer322/site-jenkins' 
            }
        }
  
            steps {
                echo 'if that step wokr maybe and this gonna work'
                sh 'docker-compouse build .'
                sh 'docker-compouse up -d '
            }
        }
    }
}
